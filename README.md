# VPN Installer Script

Универсальный bash-скрипт для установки и управления VPN. Поддерживает различные дистрибутивы Linux и предоставляет интерактивный интерфейс для управления VPN-серверами. Включает поддержку wg-easy (WireGuard через Docker) и 3x-ui (Xray Panel через официальный скрипт).

## 🚀 Возможности

- **Универсальность**: Поддержка RedHat, Ubuntu, Debian и их производных
- **Множественные VPN**: Поддержка wg-easy и 3x-ui (Xray Panel)
- **Интерактивность**: Красивое меню с цветным выводом
- **Автоматизация**: Автоматическая установка зависимостей и настройка
- **Безопасность**: Проверки системы и валидация параметров
- **Логирование**: Подробное логирование всех операций
- **Очистка**: Очистка VPN контейнеров и данных при необходимости

## 📋 Требования

- Linux дистрибутив (RedHat, Ubuntu, Debian, Arch и их производные)
- Права root (sudo)
- Подключение к интернету
- Минимум 1GB свободного места на диске

## 🛠 Поддерживаемые VPN

### wg-easy
- **Описание**: WireGuard VPN с веб-интерфейсом для управления
- **Порт по умолчанию**: 51820 (UDP) - WireGuard
- **Веб-интерфейс**: 8080 (TCP) - настраиваемый
- **Версия**: Последняя версия с Node.js v18
- **Особенности**: Простая настройка, веб-управление клиентами, настраиваемые порты
- **Примечание**: Плашка "There is an update available!" - это нормальное поведение wg-easy, которое показывает доступные обновления

### 3x-ui (Xray Panel)
- **Описание**: Продвинутая веб-панель для управления Xray-core сервером с поддержкой множественных протоколов
- **Порт по умолчанию**: 54321 (TCP) - веб-панель
- **Метод установки**: Официальный скрипт (прямая установка на систему)
- **Особенности**: 
  - Поддержка Vmess, Vless, Trojan, Shadowsocks, Wireguard
  - Многопользовательская система с ограничениями по трафику и времени
  - Веб-интерфейс для управления пользователями и настройками
  - Автоматическое обновление конфигураций
  - Мониторинг трафика и статистика
  - Поддержка TLS и Reality

### Планируемые:
- OpenVPN
- SoftEther VPN
- WireGuard (нативный)

## 📦 Установка

1. Скачайте скрипт:
```bash
wget https://raw.githubusercontent.com/your-repo/vpn-installer.sh
```

2. Сделайте скрипт исполняемым:
```bash
chmod +x vpn-installer.sh
```

3. Запустите скрипт с правами root:
```bash
sudo ./vpn-installer.sh
```

## 🎯 Использование

### Главное меню

После запуска скрипт автоматически:
1. Проверит права root
2. Определит операционную систему
3. Установит необходимые зависимости
4. Настроит Docker и Docker Compose
5. Откроет главное меню

```
╔══════════════════════════════════════════════════════════════╗
║                    VPN Installer v1.0.0                      ║
║              Универсальный установщик VPN                    ║
╚══════════════════════════════════════════════════════════════╝

Главное меню:

  1) Установить wg-easy
  2) Установить 3x-ui (Xray Panel)
  3) Показать статус
  4) Остановить VPN
  5) Очистка wg-easy
  6) Очистка 3x-ui
  7) Полная очистка (удалить всё)
  8) Выход

────────────────────────────────────────────────────────────
```

### Установка wg-easy

При выборе опции "Установить wg-easy":

1. **Настройка параметров**:
   - Порт WireGuard (по умолчанию: 51820)
   - Порт веб-интерфейса (по умолчанию: 8080)
   - Пароль для веб-интерфейса (автогенерация если не указан)
   - Путь для данных (по умолчанию: /opt/wg-easy)
   - Выбор IP-адреса из списка доступных интерфейсов

2. **Автоматическая установка**:
   - Создание директорий
   - Загрузка последней версии wg-easy (Node.js v18)
   - Генерация docker-compose.yml
   - Запуск контейнера
   - Настройка сети

3. **Результат**:
   - Веб-интерфейс: `http://YOUR_IP:WEB_PORT`
   - Логин: `admin`
   - Пароль: указанный или сгенерированный

### Установка 3x-ui (Xray Panel)

При выборе опции "Установить 3x-ui":

1. **Настройка параметров**:
   - Выбор IP-адреса из списка доступных интерфейсов

2. **Автоматическая установка**:
   - Загрузка официального скрипта установки
   - Прямая установка на систему
   - Настройка systemd сервиса

3. **Результат**:
   - Веб-панель: `http://YOUR_IP:54321`
   - Логин: `admin`
   - Пароль: `admin`
   - Поддержка Vmess, Vless, Trojan, Shadowsocks, Wireguard
   - Многопользовательская система

4. **Управление**:
   - Веб-интерфейс для управления пользователями
   - Настройка протоколов и ограничений
   - Мониторинг трафика и статистика
   - Поддержка TLS и Reality
   - Ограничения по трафику и времени для пользователей

## 🔧 Функции

### Системные проверки
- ✅ Проверка прав root
- ✅ Определение ОС и пакетного менеджера
- ✅ Проверка подключения к интернету
- ✅ Проверка статуса Docker
- ✅ Проверка существующих контейнеров

### Установка зависимостей
- ✅ Обновление пакетов
- ✅ Установка базовых зависимостей (curl, wget, git)
- ✅ Установка Docker
- ✅ Установка Docker Compose

### Управление VPN
- ✅ Установка wg-easy (WireGuard через Docker с Node.js v18)
- ✅ Установка 3x-ui (Xray Panel через официальный скрипт)
- ✅ Настройка параметров
- ✅ Запуск/остановка сервисов
- ✅ Просмотр статуса
- ✅ Выборочная очистка wg-easy
- ✅ Выборочная очистка 3x-ui (включая systemd сервисы)
- ✅ Полная очистка всех VPN

## 📁 Структура файлов

```
/opt/wg-easy/
├── docker-compose.yml    # Конфигурация Docker Compose
├── data/                 # Данные WireGuard
│   ├── wg0.conf         # Конфигурация сервера
│   ├── wg0.key          # Приватный ключ
│   └── db/              # База данных клиентов
└── logs/                # Логи (если включены)
```

## 🔍 Логирование

Все действия скрипта записываются в файл:
```
/tmp/vpn-installer.log
```

Формат логов:
```
[2024-01-15 10:30:45] [INFO] Определение операционной системы...
[2024-01-15 10:30:46] [SUCCESS] Обнаружена ОС: Ubuntu 22.04.3 LTS (ubuntu)
[2024-01-15 10:30:47] [SUCCESS] Пакетный менеджер: apt
```

## 🛡️ Безопасность

### Проверки безопасности
- Проверка прав root
- Валидация пользовательского ввода
- Безопасная генерация паролей
- Изоляция контейнеров

### Рекомендации
- Используйте сложные пароли
- Ограничьте доступ к веб-интерфейсу
- Регулярно обновляйте систему
- Мониторьте логи

## 🔧 Настройка

### Переменные по умолчанию
```bash
DEFAULT_VPN="wg-easy"
DEFAULT_PORT="51820"
DEFAULT_PASSWORD=""
DEFAULT_DATA_PATH="/opt/wg-easy"
```

### Изменение настроек
Отредактируйте переменные в начале скрипта для изменения значений по умолчанию.

## 🐛 Устранение неполадок

### Частые проблемы

1. **Ошибка прав доступа**
   ```bash
   sudo ./vpn-installer.sh
   ```

2. **Docker не запускается**
   ```bash
   sudo systemctl start docker
   sudo systemctl enable docker
   ```

3. **Порт занят**
   - Измените порт в настройках
   - Проверьте: `netstat -tulpn | grep :51820`

4. **Проблемы с сетью**
   ```bash
   # Проверка firewall
   sudo ufw status
   sudo ufw allow 51820/udp
   sudo ufw allow 8080/tcp
   ```

### Логи для диагностики
```bash
# Логи скрипта
tail -f /tmp/vpn-installer.log

# Логи Docker
docker logs wg-easy

# Логи системы
journalctl -u docker
```

## 📝 Примеры использования

### Быстрая установка wg-easy
```bash
sudo ./vpn-installer.sh
# Выберите опцию 1
# Используйте значения по умолчанию
```

### Быстрая установка 3x-ui
```bash
sudo ./vpn-installer.sh
# Выберите опцию 2
# Используйте значения по умолчанию
```

### Установка с кастомными параметрами
```bash
sudo ./vpn-installer.sh
# Выберите опцию 1
# Укажите:
# - Порт: 51821
# - Пароль: MySecurePassword123
# - Путь: /opt/my-vpn
# - Выберите IP из списка доступных интерфейсов
```

### Проверка статуса
```bash
sudo ./vpn-installer.sh
# Выберите опцию 2
```

### Обновление wg-easy (вручную)
Если вы хотите обновить wg-easy до последней версии:

```bash
# Остановить и удалить контейнер
cd /opt/wg-easy
docker-compose down

# Загрузить новую версию
docker pull weejewel/wg-easy:latest

# Запустить обновленную версию
docker-compose up -d
```

### Очистка VPN

#### Очистка wg-easy
```bash
sudo ./vpn-installer.sh
# Выберите опцию 5
# Подтвердите удаление
```

#### Очистка 3x-ui
```bash
sudo ./vpn-installer.sh
# Выберите опцию 6
# Подтвердите удаление
```

#### Полная очистка (все VPN)
```bash
sudo ./vpn-installer.sh
# Выберите опцию 7
# Подтвердите удаление
```

## 🤝 Поддержка

### Поддерживаемые дистрибутивы
- **RedHat/CentOS/Rocky/Alma**: dnf/yum
- **Ubuntu/Debian/Linux Mint**: apt
- **Arch/Manjaro**: pacman
- **Fedora**: dnf

### Системные требования
- **CPU**: 1 ядро
- **RAM**: 512MB
- **Диск**: 1GB свободного места
- **Сеть**: Статический IP (рекомендуется)

## 📄 Лицензия

MIT License - см. файл LICENSE для подробностей.

## 🔄 Обновления

### Версия 1.1.0
- ✅ Добавлена поддержка 3x-ui (Xray Panel)
- ✅ Установка 3x-ui через официальный скрипт
- ✅ Обновленное главное меню с поддержкой двух VPN
- ✅ Улучшенная функция статуса для отображения всех VPN
- ✅ Выборочная очистка wg-easy и 3x-ui
- ✅ Полная очистка 3x-ui (включая systemd сервисы и пользователей)

### Версия 1.0.0
- ✅ Базовая поддержка wg-easy
- ✅ Универсальная установка Docker
- ✅ Интерактивное меню
- ✅ Логирование операций
- ✅ Функция очистки VPN контейнеров

### Планы развития
- 🔄 Поддержка OpenVPN
- 🔄 Поддержка SoftEther VPN
- 🔄 Автоматические обновления
- 🔄 Веб-интерфейс управления
- 🔄 API для интеграции

## 📞 Контакты

- **Issues**: GitHub Issues
- **Discussions**: GitHub Discussions
- **Email**: your-email@example.com

---

**Примечание**: Этот скрипт предназначен для образовательных целей. Используйте VPN ответственно и в соответствии с местным законодательством.
